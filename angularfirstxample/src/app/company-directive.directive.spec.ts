import { CompanyDirectiveDirective } from './company-directive.directive';

describe('CompanyDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CompanyDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
