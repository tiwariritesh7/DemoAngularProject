import { CompanyPipePipe } from './company-pipe.pipe';

describe('CompanyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
